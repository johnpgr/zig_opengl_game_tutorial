{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Zig OpenGL Game",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/zig-out/bin/zig_opengl_game",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "console": "integratedTerminal",
            "preLaunchTask": "build (debug)"
        }
    ]
}