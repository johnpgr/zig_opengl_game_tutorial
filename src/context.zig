/// Context is a shared struct that is used from the main entrypoint and the shared game library.
/// It persists for the lifetime of the application and is used to store global state.
pub const Context = extern struct {
};
